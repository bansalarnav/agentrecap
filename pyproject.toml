[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "agentrecap"
version = "0.8.0"
description = "Analyze local Codex, Claude Code, OpenCode, and pi sessions"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "certifi>=2024.8.30",
    "jinja2>=3.1.0",
    "matplotlib>=3.10.0",
    "numpy>=2.2.0",
    "pandas>=2.3.0",
]

[project.scripts]
agentrecap = "agentrecap.cli:main"

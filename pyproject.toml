[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "agentrecap"
version = "0.11.1"
description = "Analyze local Codex, Claude Code, OpenCode, pi, and Oh My Pi sessions"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "certifi>=2024.8.30",
    "flask>=3.0",
    "jinja2>=3.1.0",
    "matplotlib>=3.10.0",
    "numpy>=2.2.0",
    "pandas>=2.3.0",
]

[project.scripts]
agentrecap = "agentrecap.cli:main"
